# 給後續維護者與 AI 助手的約定

這份文件只寫「不要改」與「為什麼」。功能說明請看 README.md。

## 三個不可動搖的約定

### 1. 座標一律是相對百分比 0 到 100

所有標註的 `x y w h` 都是相對於該聯底圖的百分比。不是像素，不是 0 到 1。

理由是同一份標註要同時在手機、桌機與 A4 列印上落在同一個位置，
而百分比可以直接餵給 CSS 的 `left/top/width/height`，不需要任何換算。

AI 回傳的 `box_2d` 是 0 到 1000，轉換只發生在 `src/lib/ai.ts` 的 `boxToRegionRect()` 一個地方。
不要在其他任何檔案做座標換算。

**同一個專案裡混用兩套座標系，是這類疊圖產品最經典的長期 bug 來源。**
如果你想改成 0 到 1，答案是不要。

### 2. 路由用 hash，不用 history

這樣同一份建置產物可以放在 Vercel 根目錄、GitHub Pages 子路徑、
學校內網的某個資料夾，甚至用 `file://` 直接開啟，都不需要伺服器端 rewrite。

`vite.config.ts` 的 `base: './'` 是同一件事的另一半，也不要改成絕對路徑。

### 3. 建置產物必須是單一 JS 檔

`vite.config.ts` 裡的 `codeSplitting: false` 不是效能設定，是功能需求。
「下載離線版」是把目前頁面自己的 JS 與 CSS 抓下來內嵌進一份新的 HTML，
一旦有 code splitting，那份離線 HTML 就會缺 chunk 而開不起來。

新增功能時不要用 `import()` 動態載入。

## 幾個容易踩的地方

**iOS Safari 的 canvas 不能編碼 WebP。**
`src/lib/image.ts` 的 `canEncodeWebp()` 會偵測並退回 JPEG，不要拿掉。

**OpenAI 在金鑰無效時是在邊緣節點擋掉請求，回應不帶 CORS 標頭。**
瀏覽器只會丟出一個沒有內容的 `TypeError`，讀不到狀態碼也讀不到訊息。
`src/lib/ai.ts` 已經把它翻譯成「API 金鑰可能無效」，這個 catch 不要刪。
Gemini 與 Anthropic 在同樣情況下會回可讀的 4xx。

**Gemini 的 `box_2d` 是 `[ymin, xmin, ymax, xmax]`，y 在前。**
而且那個 1000×1000 的座標空間是被拉伸套在圖上的，不保留長寬比，
所以 x 乘寬、y 乘高要各自獨立換算，不要做 letterbox 補正。

**模擬填寫的產出一定要有浮水印。**
`drawSampleWatermark()` 與畫布上那層 SAMPLE 文字不是裝飾。
模擬圖在像素上跟一張真的填好的表單照片沒有差別，一旦存進相簿就會被轉傳、被誤用。

**PDF 主要路徑是瀏覽器原生列印，不是 jsPDF。**
中文在原生列印裡是作業系統排版的向量文字，不需要內嵌任何字型檔。
jsPDF 與 pdfmake 都沒有執行期子集化，pdf-lib 的中文子集化有已知未修的缺字問題。
`buildPdfBlob()` 只用來組圖片，刻意完全不呼叫 `embedFont`。

## 誠實原則

這個專案有幾個地方是刻意「做得不夠強，但講清楚」，不要把它們改成假裝很強：

- 處室密碼是前端 SHA-256 比對，可以被繞過。它是防呆，不是資安。
  不要改成 AES 加密，那只會給人「這是安全的」的錯覺，而且與單檔離線版互斥。
- 學號推導會附上「為什麼這樣猜」的理由。不要拿掉理由改成直接給答案。
  推導錯的資訊比沒有資訊更危險，使用者需要能自己判斷。
- 首頁明講「清除瀏覽器資料引導就會消失」。不要為了畫面好看把這段拿掉。
- QR 塞不下整份引導，這是物理限制。不要在介面上暗示它做得到。
